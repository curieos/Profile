<% content_for :title do %>
	<%= @post.title %> - Curieos
<% end %>

<% content_for :content do %>
<div class="card">
    <header class="card-header text-center">
        <h2><%= @post.title %></h2>
    </header>
    <div class="card-body">
		<p>
			<%= @post.body %>
		</p>
	</div>
</div>
<% end %>