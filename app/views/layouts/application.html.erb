<!DOCTYPE html>
<html>

<head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "CurieOS" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "CurieOS Profile Website" %>">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
</head>

<body class="container p-0">
    <div class="p-2 navbar-side-container">
        <%= render  "layouts/navigation" %>
    </div>
    <main class="p-2 content" role="main">
        <%= content_for?(:content) ? yield(:content) : yield %>
    </main>
    <%= javascript_pack_tag 'nav' %>
</body>

</html>